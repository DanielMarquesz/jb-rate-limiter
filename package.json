{
  "name": "jb-rate-limiter",
  "version": "1.0.0",
  "description": "Sliding-window rate limiter with per-user history",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
